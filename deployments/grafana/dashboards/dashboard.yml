apiVersion: 1

providers:
  - name: 'cloudsentry-dashboards'
    org_id: 1
    folder: ''
    type: file
    disable_deletion: false
    update_interval_seconds: 10
    allow_ui_updates: true
    options:
      path: /etc/grafana/provisioning/dashboards
